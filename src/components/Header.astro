---
import { getRelativeLocaleUrl } from "astro:i18n";
import IconMenuDeep from "@/assets/icons/IconMenuDeep.svg";
import IconGhost from "@/assets/icons/IconGhost.svg";
import IconSunHigh from "@/assets/icons/IconSunHigh.svg";
import IconSearch from "@/assets/icons/IconSearch.svg";
import IconMoon from "@/assets/icons/IconMoon.svg";
import IconX from "@/assets/icons/IconX.svg";
import LinkButton from "./LinkButton.astro";
import { stripBase, stripLocale } from "@/utils/withBase";
import { useTranslations } from "@/i18n";
import config from "@/config";

const { site, features } = config;

const locale = Astro.currentLocale ?? config.site.lang;
const t = useTranslations(locale);
const publicationName = site.title.split("·")[0].trim();
// Strip the base prefix so isActive comparisons work regardless of deploy subdirectory.
// e.g. with base "/blog": "/blog/posts/slug" → "/posts/slug"
const relativePath = stripBase(Astro.url.pathname);
const pathWithoutTrailingSlash =
  relativePath.endsWith("/") && relativePath !== "/"
    ? relativePath.slice(0, -1)
    : relativePath;

const currentPath = stripLocale(pathWithoutTrailingSlash, locale);

const isActive = (path: string) => {
  const currentPathArray = currentPath.split("/").filter(p => p.trim());
  const pathArray = path.split("/").filter(p => p.trim());
  return currentPath === path || currentPathArray[0] === pathArray[0];
};
---

<a
  id="skip-to-content"
  href="#main-content"
  class="bg-background text-accent absolute inset-s-16 -top-full z-50 px-3 py-2 backdrop-blur-lg transition-all focus:top-4"
>
  {t.a11y.skipToContent}
</a>

<header
  class="app-layout flex flex-col items-center justify-between sm:flex-row"
>
  <div
    class="border-border bg-background relative flex w-full flex-col items-center border-b py-4 sm:pt-6 sm:pb-3"
  >
    <nav
      id="nav-menu"
      class="flex w-full flex-col items-center sm:flex-row sm:py-0"
    >
      <button
        id="menu-btn"
        class="focus-outline self-end p-2 sm:hidden"
        aria-label={t.a11y.openMenu}
        aria-expanded="false"
        aria-controls="menu-items"
        data-label-open={t.a11y.openMenu}
        data-label-close={t.a11y.closeMenu}
      >
        <IconX id="close-icon" class="hidden" />
        <IconMenuDeep id="menu-icon" />
      </button>
      <div
        id="menu-items"
        class="mt-4 hidden w-44 gap-2 sm:mt-0 sm:flex sm:w-full sm:items-center sm:justify-between"
      >
        <ul
          class="sm:[&>li:not(:last-child)]:after:text-muted-foreground/60 [&>li>a]:text-accent [&>li>a]:hover:text-foreground grid grid-cols-1 place-content-center gap-2 sm:flex sm:gap-0 sm:[&>li]:flex sm:[&>li]:items-center sm:[&>li:not(:last-child)]:after:mx-3 sm:[&>li:not(:last-child)]:after:content-['·'] [&>li>a]:block [&>li>a]:px-4 [&>li>a]:py-3 [&>li>a]:text-center [&>li>a]:text-base [&>li>a]:font-semibold [&>li>a]:tracking-tight [&>li>a]:transition-colors sm:[&>li>a]:inline-block sm:[&>li>a]:px-0 sm:[&>li>a]:py-1"
        >
          <li>
            <a
              href={getRelativeLocaleUrl(locale, "")}
              class="font-prose! text-foreground! hover:text-accent! text-xl! font-bold!"
              class:list={{ "active-nav": currentPath === "/" }}
            >
              {publicationName}
            </a>
          </li>
          <li>
            <a
              href={getRelativeLocaleUrl(locale, "posts")}
              class:list={{ "active-nav": isActive("/posts") }}
            >
              {t.nav.posts}
            </a>
          </li>
          <li>
            <a
              href={getRelativeLocaleUrl(locale, "papers")}
              class:list={{ "active-nav": isActive("/papers") }}
            >
              {t.nav.papers}
            </a>
          </li>
          <li>
            <a
              href={getRelativeLocaleUrl(locale, "lab-notes")}
              class:list={{ "active-nav": isActive("/lab-notes") }}
            >
              {t.nav.labNotes}
            </a>
          </li>
          <li>
            <a
              href={getRelativeLocaleUrl(locale, "about")}
              class:list={{ "active-nav": isActive("/about") }}
            >
              {t.nav.about}
            </a>
          </li>
        </ul>
        <ul class="flex items-center justify-center sm:gap-1">
          {
            features.search !== false && (
              <li class="flex items-center justify-center">
                <LinkButton
                  href={getRelativeLocaleUrl(locale, "search")}
                  class:list={[
                    "focus-outline text-muted-foreground hover:text-accent relative size-8",
                  ]}
                  title={t.nav.search}
                  aria-label={t.nav.search}
                >
                  <IconSearch class="absolute top-1/2 left-1/2 size-6 -translate-x-1/2 -translate-y-1/2" />
                  <span class="sr-only">{t.nav.search}</span>
                </LinkButton>
              </li>
            )
          }
          <li class="flex items-center justify-center">
            <button
              type="button"
              class="focus-outline text-muted-foreground hover:text-accent relative size-12 p-3 transition-colors sm:size-8 sm:p-1"
              title="Analytics preference"
              aria-label="Analytics preference"
              data-analytics-toggle
            >
              <IconGhost class="size-full" />
            </button>
          </li>
          {
            features.lightAndDarkMode && (
              <li class="flex items-center justify-center">
                <button
                  id="theme-btn"
                  class="focus-outline text-muted-foreground hover:[&>svg]:stroke-accent relative size-12 p-4 sm:size-8"
                  title={t.a11y.toggleTheme}
                  aria-label="auto"
                  aria-live="polite"
                >
                  <IconMoon class="absolute top-[50%] left-[50%] translate-[-50%] scale-100 rotate-0 transition-all dark:scale-0 dark:-rotate-90" />
                  <IconSunHigh class="absolute top-[50%] left-[50%] translate-[-50%] scale-0 rotate-90 transition-all dark:scale-100 dark:rotate-0" />
                </button>
              </li>
            )
          }
        </ul>
      </div>
    </nav>
  </div>
</header>

<div
  class="bg-foreground text-background pointer-events-none fixed right-4 bottom-4 z-50 translate-y-2 px-3 py-2 text-sm opacity-0 shadow-sm transition-[opacity,transform] duration-200"
  role="status"
  aria-live="polite"
  aria-atomic="true"
  data-analytics-toast
>
</div>

<script>
  function toggleNav() {
    const menuBtn = document.querySelector<HTMLButtonElement>("#menu-btn");
    const menuItems = document.querySelector("#menu-items");
    const menuIcon = document.querySelector("#menu-icon");
    const closeIcon = document.querySelector("#close-icon");

    if (!menuBtn || !menuItems || !menuIcon || !closeIcon) return;

    const openLabel = menuBtn.dataset.labelOpen ?? "Open menu";
    const closeLabel = menuBtn.dataset.labelClose ?? "Close menu";

    menuBtn.addEventListener("click", () => {
      const openMenu = menuBtn.getAttribute("aria-expanded") === "true";

      menuBtn.setAttribute("aria-expanded", openMenu ? "false" : "true");
      menuBtn.setAttribute("aria-label", openMenu ? openLabel : closeLabel);

      menuItems.classList.toggle("hidden");
      menuItems.classList.toggle("grid");
      menuIcon.classList.toggle("hidden");
      closeIcon.classList.toggle("hidden");
    });
  }

  toggleNav();

  document.addEventListener("astro:after-swap", toggleNav);
</script>

<script>
  import {
    ANALYTICS_PREFERENCE_EVENT,
    analyticsDisabled,
    toggleAnalytics,
    type AnalyticsPreference,
  } from "@/scripts/analyticsPreference";

  let toastTimer: number | undefined;

  function reflectAnalyticsToggle(disabled = analyticsDisabled()): void {
    const button = document.querySelector<HTMLButtonElement>(
      "[data-analytics-toggle]"
    );
    if (!button) return;

    const label = disabled
      ? "Analytics disabled — click to enable"
      : "Analytics enabled — click to disable";

    button.title = label;
    button.setAttribute("aria-label", label);
    button.dataset.analyticsDisabled = String(disabled);
    button.classList.toggle("text-accent", disabled);
    button.classList.toggle("text-muted-foreground", !disabled);
    button
      .querySelector("[data-analytics-disabled-mark]")
      ?.classList.toggle("hidden", !disabled);
  }

  function showAnalyticsToast(disabled: boolean): void {
    const toast = document.querySelector<HTMLElement>("[data-analytics-toast]");
    if (!toast) return;

    window.clearTimeout(toastTimer);
    toast.textContent = disabled ? "Analytics disabled" : "Analytics enabled";
    toast.classList.remove("translate-y-2", "opacity-0");

    toastTimer = window.setTimeout(() => {
      toast.classList.add("translate-y-2", "opacity-0");
    }, 1800);
  }

  function setupAnalyticsToggle(): void {
    reflectAnalyticsToggle();

    const button = document.querySelector<HTMLButtonElement>(
      "[data-analytics-toggle]"
    );
    if (!button || button.dataset.preferenceBound === "true") return;

    button.dataset.preferenceBound = "true";
    button.addEventListener("click", () => {
      showAnalyticsToast(toggleAnalytics());
    });
  }

  setupAnalyticsToggle();
  document.addEventListener("astro:after-swap", setupAnalyticsToggle);
  window.addEventListener(ANALYTICS_PREFERENCE_EVENT, event => {
    reflectAnalyticsToggle(
      (event as CustomEvent<AnalyticsPreference>).detail.disabled
    );
  });
</script>
